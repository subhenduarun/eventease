name: EventEase Deployment

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: |
        pytest  # or python -m unittest

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    # 🚫 Skipping Docker login step temporarily
    # - name: Login to Docker
    #   uses: docker/login-action@v2
    #   with:
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build Docker image
      run: |
        docker build -t eventease:latest .

    # Optional: skip push step too if login is disabled
    # - name: Push Docker image
    #   run: |
    #     docker tag eventease:latest your-dockerhub-username/eventease:latest
    #     docker push your-dockerhub-username/eventease:latest
